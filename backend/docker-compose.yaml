version: '3.8'

name: csd-shs

services:
  csd_backend:
    build:
      context: .
      dockerfile: webapp/Dockerfile
    container_name: csd_backend
    depends_on:
      - csd_database
    ports:
      - "8080:8080"
    environment:
      SPRING_DATASOURCE_URL:
      SPRING_DATASOURCE_USERNAME:
      SPRING_DATASOURCE_PASSWORD:
      GITHUB_CLIENT_ID:
      GITHUB_CLIENT_SECRET:
      GITHUB_REDIRECT_URI:
      NEXTCLOUD_CLIENT_ID:
      NEXTCLOUD_CLIENT_SECRET:
      NEXTCLOUD_REDIRECT_URI:
      NEXTCLOUD_URL:
      APP_OAUTH2_SUCCESS_REDIRECT_URL:
      APP_OAUTH2_FAIL_REDIRECT_URL:
      APP_FRONTEND_URL:
      APP_AUTH_JWT_SECRET:
    networks:
      - csd_net
  csd_database:
    image: mariadb:10.3
    container_name: csd_database
    volumes:
      - ./db:/docker-entrypoint-initdb.d
    environment:
      MYSQL_ROOT_PASSWORD: secret
    networks:
        - csd_net

networks:
  csd_net:
    driver: bridge
